@games.map do |game|
  {
    id: game.id,
    game_creator: game.player.name,
    field_name: game.field.name,
    date_and_time: game.friendly_date_time!,
    players_attending: game.players_attending.length, # I only want to show players whose attending status is true
  }
end
