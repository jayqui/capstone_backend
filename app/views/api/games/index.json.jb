@games.each do |game|
  {
    id: game.id,
    player_id: game.player_id,
    field_id: game.field_id,
    date_time: game.date_time,
    attending: game.players_attending, # This line of code doesn't show up on server. Why not?
  }
end
